.DS_Store
**/.DS_Store

# skills are part of the project (tracked); other local .claude config stays ignored
.claude/*
!.claude/skills/

# paper-notes working dir (inbox + raw figure sources, heavy/transient) — only the
# published WebP under <slug>/ is committed; see pipeline/README.md
_src/
